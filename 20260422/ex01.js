function solution(num1, num2) {;
    return Math.floor(num1 / num2);
}

// Document 브라우저 프론트 html

// Math Object 자료형 node 기본 지원 되는 객체
// round 반올림
// floor 내림
// ceil 올림
// random() 0<x<1사이의 랜덤한수

const ret = solution(10, 4)
console.log(ret);